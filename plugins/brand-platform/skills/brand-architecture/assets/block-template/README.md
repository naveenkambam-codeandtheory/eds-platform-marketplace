# example

One line on what this block does and when an author should reach for it.

## Authoring shape

| Column 1 | Column 2 |
|---|---|
| Image | Text |

## Variants

| Variant | Effect |
|---|---|
| `compact` | Tighter padding and gap |

## Component tokens

| Token | Default | Notes |
|---|---|---|
| `--example-bg` | `var(--color-surface-raised)` | Card background |
| `--example-radius` | `var(--radius-m)` | Corner radius |
| `--example-padding` | `var(--space-4)` | Inner padding |

## Brand notes

Which brands use it, which capability flags it reads, and any brand-scoped
overrides currently in the CSS.

## Fork status

Shared. (If a brand diverges at L4: add `<brand>/<brand>.js` — and `<brand>/<brand>.css`
if styling diverges too — under this folder; this block's own name, model, and filter
entry stay unchanged. Link the ADR in that fork file's header, name the owner, state the
re-merge review date and what would let us collapse it back.)
